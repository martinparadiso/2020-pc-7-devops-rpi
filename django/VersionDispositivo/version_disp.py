#!/usr/bin/python3
from bs4 import BeautifulSoup
import requests


def version_disp():
 #ulr = f'http://{ip}:1221/version'
 #resultado = resultado.json()['version']
 resultado = 1
 return(resultado)  
